defaults:
  - hydra: default
  - logger: mlflow
  - /data/datasets@data.train: stain_normalization/train
  - /data/datasets@data.val: stain_normalization/val
  - /data/datasets@data.test: stain_normalization/test
  - /data/datasets@data.predict: stain_normalization/predict
  - /model/backbone: resnet50
  - /model/decode_head: unet2048
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: null

trainer:
  enable_checkpointing: True
  max_epochs: 30
  log_every_n_steps: 5
  val_check_interval: 0.5
  callbacks:
    - _target_: lightning.pytorch.callbacks.ModelCheckpoint
      save_top_k: 1
      monitor: validation/loss
      mode: min

data:
  batch_size: 64
  num_workers: 6

metadata:
  user: xlopatka
  experiment_name: Stain-Normalization
  run_name: Unet Train Stain Normalization
  description: Testing Stain Normalization
  hyperparams: {}

output_dir: ./data





